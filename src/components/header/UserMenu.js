import React from "react";
import { Link } from "react-router-dom";

const UserMenu = () => {
  return (
    <div className="menu">
      <Link to="/" className="mmen_header hover3"></Link>
    </div>
  );
};

export default UserMenu;
